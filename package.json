{
  "name": "mg",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "25.9.6",
    "typescript": "6.0.3",
    "vitest": "5.0.0"
  }
}
